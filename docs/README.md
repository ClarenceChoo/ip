# CHOO User Guide

CHOO helps you manage todos, deadlines, and events by typing short commands.
This guide takes you from your first task to finding, sorting, and completing
your work. All aboard!

## On this page

- [Getting started](#getting-started)
- [Understanding the task list](#understanding-the-task-list)
- [Adding a todo](#adding-a-todo), [deadline](#adding-a-deadline), or [event](#adding-an-event)
- [Listing tasks](#listing-tasks)
- [Marking a task as done](#marking-a-task-as-done) or [not done](#marking-a-task-as-not-done)
- [Deleting a task](#deleting-a-task)
- [Finding tasks](#finding-tasks)
- [Sorting tasks by deadline](#sorting-tasks-by-deadline)
- [Handling invalid commands](#handling-invalid-commands)
- [Exiting CHOO](#exiting-choo)
- [Command summary](#command-summary)

## Getting started

1. Install Java 25. Run `java -version` in a terminal to check your version.
2. Download the `choo.jar` asset from the [latest CHOO release](https://github.com/ClarenceChoo/ip/releases/latest).
3. Put the JAR in a folder where you can save files, then open a terminal in that folder.
4. Run `java -jar "choo.jar"` to open CHOO.

The JAR includes JavaFX for Windows/Linux x64 and Intel/Apple Silicon macOS.
You do not need to install JavaFX separately.

The CHOO window opens with a welcome message. Enter these commands one at a time:

1. `todo Read lecture notes` — CHOO confirms that it added the task.
2. `list` — your new task appears as `1.[T][ ] Read lecture notes`.
3. `mark 1` — CHOO confirms completion; `list` now shows `[T][X]`.

Commands are lowercase. Replace uppercase placeholders such as `DESCRIPTION`
with your own text; do not type the placeholder names.

Enter one command at a time in the text field, then press Enter or click
**Send**. Enter `bye` or close the window when you want to exit.

CHOO saves the task list automatically after every successful change and
loads it the next time the chatbot starts. Data is stored relative to the
working directory at `data/choo.txt`; the directory and file are created
automatically when needed. Do not edit this file while CHOO is running.

Keep using the same folder when launching CHOO so it finds your saved tasks.
To back up your tasks, close CHOO and copy `data/choo.txt` to a safe location.

## Understanding the task list

Each task starts with its type and completion status:

- `[T]` identifies a todo.
- `[D]` identifies a deadline.
- `[E]` identifies an event.
- `[X]` means the task is done, while `[ ]` means it is not done.

## Adding a todo

Use `todo DESCRIPTION` for a task without a date or time.

Example: `todo read book`

CHOO confirms the new `[T][ ] read book` task and shows the updated task count.
Adding the same description again creates a separate task.

## Adding a deadline

Use `deadline DESCRIPTION /by DATE_OR_TIME` for a task that must be completed
by a particular date or time. Enter either `yyyy-MM-dd` for a date or
`yyyy-MM-dd HHmm` for a date and 24-hour time.

Examples:

- `deadline return book /by 2019-12-02`
- `deadline submit report /by 2019-12-02 1800`

CHOO displays these deadlines as `Dec 2 2019` and
`Dec 2 2019, 6:00PM`, respectively. Impossible dates and unsupported formats
are rejected.

A date-only deadline remains date-only. If you enter midnight explicitly,
such as `2019-12-02 0000`, CHOO displays it as `Dec 2 2019, 12:00AM`.

Deadline rows saved by an older CHOO version with free-form dates (such as
`Friday`) cannot be converted safely. If CHOO reports one, close CHOO,
back up the file, and edit that row in `data/choo.txt` to use
`yyyy-MM-dd` or `yyyy-MM-dd HHmm`;
CHOO reports the affected line and leaves the file unchanged.

## Adding an event

Use `event DESCRIPTION /from START /to END` for a task with a start and end.

Example: `event project meeting /from Monday 2pm /to 4pm`

The start and end values are free-form text. CHOO does not check their
chronological order or include events in deadline sorting.
The example appears as `[E][ ] project meeting (from: Monday 2pm to: 4pm)`.

## Listing tasks

Use `list` to display all tasks and their task numbers.

Example output:

```text
1.[T][ ] read book
2.[D][X] return book (by: Dec 2 2019)
3.[E][ ] project meeting (from: Monday 2pm to: 4pm)
```

## Marking a task as done

Use `mark NUMBER`, where `NUMBER` is the task number shown by `list`.

Example: `mark 2`

CHOO confirms the task is done, and its status changes to `[X]`.

## Marking a task as not done

Use `unmark NUMBER`, where `NUMBER` is the task number shown by `list`.

Example: `unmark 2`

CHOO confirms the task is not done, and its status changes to `[ ]`.

## Deleting a task

Use `delete NUMBER`, where `NUMBER` is the task number shown by `list`.

Example: `delete 2`

CHOO shows the removed task and the new task count. The remaining tasks are
renumbered automatically. Deletion is saved immediately and cannot be undone
with a command; check the task number using `list` first.

## Finding tasks

Use `find KEYWORD` to display tasks whose descriptions contain `KEYWORD`.
Matching is case-sensitive, and results keep the same order as the task list.
Dates, times, and task markers are not searched.
If there are no matches, CHOO shows the matching-stops heading with no tasks.

> **Task numbers:** Search results are numbered from 1 within the results.
> These numbers may differ from the full task list. Run `list` before using
> `mark`, `unmark`, or `delete`; those commands always use full-list numbers.

Example: `find book`

Example output:

```text
Here are the matching stops:
1.[T][ ] read book
2.[D][X] return book (by: Dec 2 2019)
```

## Sorting tasks by deadline

Use `sort` to place deadlines first, ordered from earliest to latest. Todos
and events are placed after the deadlines while retaining their existing
relative order. Deadlines with the same date and time also retain their
relative order.

The sorted order is saved automatically and is used for future task numbers.
Date-only deadlines sort as midnight, before later times on the same date.
CHOO displays the reordered list after sorting.

Example: `sort`

## Handling invalid commands

CHOO explains invalid commands and missing task details without changing your
existing task list. It also reports unreadable or corrupted task data instead
of crashing. Correct the command or data-file problem and try again.

An empty submission in the GUI is ignored; the console reports an error.
The `list`, `sort`, and `bye` commands do not
accept extra details. A deadline must contain exactly one standalone `/by`
delimiter. An event must contain exactly one standalone `/from` delimiter,
followed by exactly one standalone `/to` delimiter.

Use spaces between the command and its details. Leading and trailing spaces
are ignored. Descriptions must not be empty. Put spaces around `/by`, `/from`,
and `/to` when using them to separate fields.

| Problem or message | What to do |
| --- | --- |
| `I don't recognize that command.` | Check the spelling and use lowercase commands, such as `todo`, not `Todo`. |
| `A todo needs a description.` | Add task details, for example `todo Read lecture notes`. |
| `Use yyyy-MM-dd or yyyy-MM-dd HHmm for a deadline date.` | Use a real date and optional 24-hour time, for example `2026-09-18 1800`. |
| `Task number ... is outside the list.` | Run `list` and choose a displayed number starting from 1. |
| `I couldn't save the task data file.` | Check folder permissions and the storage path, then retry the command. The change was not applied. |

If your list unexpectedly appears empty, check that you launched CHOO from
the same folder as before: it reads `data/choo.txt` relative to that folder.
A missing file starts a new empty list. To recover existing tasks, close
CHOO and restore your backed-up file before restarting.

If reading or saving fails, check that the folder is writable and
`data/choo.txt` is a file, not a directory. A failed save leaves the task list
unchanged. For corrupted data, back up the file before correcting the
reported line, then restart CHOO.

## Exiting CHOO

Use `bye` to close the window. You can also use the window's normal close
button.

## Command summary

| Task | Command |
| --- | --- |
| Add a todo | `todo DESCRIPTION` |
| Add a deadline | `deadline DESCRIPTION /by DATE_OR_TIME` |
| Add an event | `event DESCRIPTION /from START /to END` |
| List all tasks | `list` |
| Complete a task | `mark NUMBER` |
| Reopen a task | `unmark NUMBER` |
| Delete a task | `delete NUMBER` |
| Search descriptions | `find KEYWORD` |
| Sort by deadline | `sort` |
| Exit | `bye` |

## Credits

The profile avatars were generated with OpenAI's GPT Image model.
See the project's [acknowledgements](https://github.com/ClarenceChoo/ip#acknowledgements)
for its course foundations, tools, and AI assistance.
